![Commutative diagram of quotients of the integer polynomial ring and their localizations and completions](../images/Algebraic%20Closure/quotient-localization-completion.png)

Here are some relationships:

If you have a field:

localization by 2 then completion by 2 gives the same as just completion by 2.

from the string perspective:
 - Z is finite strings. "filling in the gaps" in this completions looks like extending the length to infinity
 - Localization first gives you eventually periodic strings. Completion looks like extending the period off too infinity.

For either rep (localization and completion), taking the mod 2 (first component) returns F_2